# gnr_d20 — Mirror of gnr_d11

This directory is a placeholder mirror of `gnr_d11`.

**Do NOT develop here.** All JavaScript and CSS changes must be made
in `gnr_d11` first, tested, and only then ported here before committing.

`gnr_d11` is the runtime version loaded by the application.
