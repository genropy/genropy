⚠ Phase 1 has issues: `gnr app dbsetup gnrdevelop` cannot exit 0 on this machine

Everything the phase asked for is in place and green — `gnr_it:glbl` mounted in gnrdevelop, its tables created and loaded from the package's own startup data (110 provinces, 8092 comuni), the new `page_required_packages` guard with unit tests, the CI step, the six referendum-era model injections deleted; `test_pages_ratchet.py` + `test_pages_documented.py` + `test_pages_smoke.py` report 5 passed and flake8 is clean on the Files set. The one unmet Done criterion is `gnr app dbsetup gnrdevelop` exiting 0: it aborts on a pre-existing docu column-type drift (`ALTER TABLE "docu"."docu_documentation" ALTER TABLE base_language TYPE …`, which sqlite cannot execute) — reproduced at HEAD with instanceconfig reverted, so it is not caused by this phase, and fixing it would mean touching the docu model or the sqlite adapter, outside this phase's file set. Phase marked `[!]` with Issue/Attempted notes; work committed in `e5462996c`.
